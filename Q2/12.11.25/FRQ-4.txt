a

public static int numSymbols (String s) {
    Symbols = 0; 
    Symbols -= s.length() - (numDigits(s) - numLetters(s));
    returns Symbols;
}

b

public static String passwordStrength(String p) {
    if (p.length() >= 8) {
        if (numDigits(p) >= 1 && numLetters(p) >= 1 && numSymbols(p) >= 1) {
                    String output = "strong";
                    return output
        }
    }
    if ((p.length() >= 5 && p.length < 8) || p.length() >= 8) {

    }
}